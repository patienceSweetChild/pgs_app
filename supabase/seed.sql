-- After creating a user in Supabase Auth, promote them to super_admin:
-- replace USER_UUID and optionally display name.

-- insert into public.staff_profiles (user_id, role_key, display_name, status)
-- values ('USER_UUID', 'super_admin', 'Super Admin', 'active');
